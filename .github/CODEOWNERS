# Owners must be a GitHub user (@username) or a team (@org/team-slug) with write
# access. A bare organization name is not valid — `@ninoverse` is the org that
# owns this repo, not an owner GitHub can resolve.
#
# If you fork this template, replace @nicolapasqua99 throughout. Once the org has
# a team, prefer @ninoverse/<team-slug>: a team survives people joining and
# leaving, a personal handle does not.

# Every file, unless overridden below.
*                       @nicolapasqua99

# The fragments are the product here — a change to one reaches every repository
# that pins a release carrying it. This repository's own rules are composed from
# them like anywhere else, so the profile that selects them decides the tree.
/fragments/             @nicolapasqua99
/.agentprofile.yml      @nicolapasqua99
/AGENTS.md              @nicolapasqua99
/CLAUDE.md              @nicolapasqua99
/.agents/               @nicolapasqua99
/.claude/               @nicolapasqua99

# Supply chain and CI: a change here can weaken the merge gates.
/.github/workflows/     @nicolapasqua99
/deny.toml              @nicolapasqua99
/rust-toolchain.toml    @nicolapasqua99
/Cargo.toml             @nicolapasqua99
